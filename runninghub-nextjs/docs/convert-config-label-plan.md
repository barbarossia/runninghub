## Overview
Rename the Convert tab section header from "FPS Convert Configuration" to "Convert Configuration".

## Current State
- The Convert tab shows "FPS Convert Configuration" in the convert configuration card.

## Target State
- The Convert tab shows "Convert Configuration".

## Requirements
- Keep layout and behavior unchanged.
- Update only the displayed label.

## Implementation Phases
1. Update the title string in the convert configuration component.
2. Verify the Convert tab renders the new label.
